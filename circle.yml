machine:
  ruby:
    version: 2.5.3
database:
  override:
    - bundle exec rake db:create db:schema:load --trace
dependencies:
  pre:
    - gem install bundler
  override:
    - mv config/secrets.ci.yml config/application.yml
